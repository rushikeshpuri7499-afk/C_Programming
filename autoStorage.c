#include<stdio.h>

int main ()
{
    // this are local variables if we not specify the auto keyword but also the variable is auto
    int i ;
    auto int j ;      // auto storage class variable 
    int k ;

    return 0;
}